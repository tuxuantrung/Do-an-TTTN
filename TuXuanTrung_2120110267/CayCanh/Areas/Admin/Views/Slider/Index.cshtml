@model IEnumerable<CayCanh.Models.Slider>

@{
    ViewBag.Title = "Tất cả Slider";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
    <link rel="stylesheet" href="~/public/icon-font/fontawesome.com/css/all.min.css" />
    ;
}
<section class="content">
    <div class="card">
        <div class="card-header">
            <div class="row">
                <div class="col-md-6">
                    <strong class="text-danger">
                        TẤT CẢ SLIDER
                    </strong>
                </div>
                <div class="col-md-6 text-right">
                    <a href="@Url.Action("Create","Slider")" class="btn btn-sm btn-success"><i class="fa-solid fa-plus"></i>Thêm</a>
                    <a href="@Url.Action("Trash","Slider")" class="btn btn-sm btn-danger"><i class="fa-solid fa-trash"></i>Thùng rác</a>
                </div>
            </div>
        </div>
        <div class="card-body">
            @Html.Partial("Message")
            <table class="table table-bordered">
                <tr>
                    <th style="width:20px;" class="text-center">#</th>
                    <th style="width:100px;" class="text-center">Hình ảnh</th>
                    <th style="width:150px;" class="text-center">Tên slider</th>
                    <th style="width:100px;" class="text-center">Liên kết</th>
                    <th style="width:100px;" class="text-center">Vị trí</th>
                    <th style="width:100px;" class="text-center">Ngày tạo</th>
                    <th style="width:150px;" class="text-center">Chức năng</th>
                    <th style="width:20px;" class="text-center">Mã</th>
                </tr>
                @foreach (var item in Model)
                {
            <tr>
                <td class="text-center">
                    <input type="checkbox" />
                </td>
                <td>
                    <img src="~/public/image/slider/@item.Img" class="img-fluid"/>
                </td>
                <td>@item.Name</td>
                <td>@item.Link</td>
                <td>@item.Position</td>
                <td class="text-center">@item.CreateAt</td>
                <td class="text-center">
                    @if (item.Status == 1)
                    {
                        <a href="@Url.Action("Status","Slider",new { id = item.Id })
                                   " class="btn btn-sm btn-success">
                            <i class="fa-solid fa-toggle-on"></i>
                        </a>
                    }
                    else
                    {
                        <a href="@Url.Action("Status","Slider",new { id = item.Id })
                                   " class="btn btn-sm btn-danger">
                            <i class="fa-solid fa-tongle-off"></i>
                        </a>
                    }
                    <a href="@Url.Action("Edit","Slider",new { id = item.Id })" class="btn btn-sm btn-success">
                        <i class="fa-solid fa-pen-to-square"></i>
                    </a>
                    <a href="@Url.Action("Details","Slider",new { id = item.Id })" class="btn btn-sm btn-info">
                        <i class="fa-solid fa-eye"></i>
                    </a>
                    <a href="@Url.Action("Delete","Slider",new { id = item.Id })" class="btn btn-sm btn-danger">
                        <i class="fa-solid fa-trash"></i>
                    </a>

                </td>
                <td class="text-center">@item.Id</td>
            </tr>
                }
            </table>
        </div>
        <div class="card-footer text-right">
            <a href="@Url.Action("Create","Slider")" class="btn btn-sm btn-success"><i class="fa-solid fa-plus"></i>Thêm</a>
            <a href="@Url.Action("Trash","Slider")" class="btn btn-sm btn-danger"><i class="fa-solid fa-trash"></i>Thùng rác</a>
        </div>
    </div>
</section>
